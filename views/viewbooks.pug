extends layout


block content
  h1= title
  p Welcome to #{title}
  table
    each book in books
        p #{book.title}
        p #{book.authors}
        p #{book.copyright}
        p #{book.edition}
        p #{book.published}